{
  "name": "focusly-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "release": "changeset version && pnpm -r build && changeset publish",
    "copy-readme": "node tooling/release/copy-readme.mjs"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "tsup": "^8.5.0",
    "typescript": "~5.9.3"
  }
}