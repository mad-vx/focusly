name: Angular compatibility (build libs)

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-libs:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        angular: [19, 20, 21]

    defaults:
      run:
        working-directory: focusly

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (Angular 21 needs >=20.19.0)
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: focusly/package-lock.json

      # For Angular 21: use the repo's real lockfile (fast + deterministic)
      - name: Install deps (v21 uses lockfile)
        if: matrix.angular == 21
        run: npm ci

      # For Angular 19/20: rewrite deps + fresh install (pinned versions)
      - name: Set Angular deps to v${{ matrix.angular }}
        if: matrix.angular != 21
        run: |
          node -e "
            const fs = require('fs');
            const path = require('path');

            const major = Number('${{ matrix.angular }}');

            // Pin exact Angular patch versions to keep npm resolution deterministic
            const angularVersionMap = {
              19: '19.2.18',
              20: '20.3.16',
            };

            const ngExact = angularVersionMap[major];
            if (!ngExact) throw new Error('No pinned Angular version for major ' + major);

            const zorro = '^' + major + '.0.0';

            // TypeScript choices for your lanes
            const ts =
              major === 19 ? '~5.6.3' :
              '~5.9.3';

            const zone = '~0.15.0';

            const angularRuntime = [
              '@angular/animations',
              '@angular/common',
              '@angular/compiler',
              '@angular/core',
              '@angular/forms',
              '@angular/platform-browser',
              '@angular/platform-browser-dynamic',
              '@angular/router'
            ];

            const angularTooling = [
              '@angular/build',
              '@angular/cli',
              '@angular/compiler-cli',
              'ng-packagr'
            ];

            const angularToolkits = [
              '@angular/material',
              '@angular/cdk'
            ];

            const setIfPresent = (obj, name, value) => {
              if (obj && obj[name]) obj[name] = value;
            };

            const patch = (pkgPath) => {
              const pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf8'));

              // runtime deps
              for (const p of angularRuntime) setIfPresent(pkg.dependencies, p, ngExact);

              // tooling deps
              for (const p of angularTooling) setIfPresent(pkg.devDependencies, p, ngExact);

              // material/cdk can be in deps OR devDeps depending on package
              for (const p of angularToolkits) {
                setIfPresent(pkg.dependencies, p, ngExact);
                setIfPresent(pkg.devDependencies, p, ngExact);
              }

              // alignment
              setIfPresent(pkg.dependencies, 'zone.js', zone);
              setIfPresent(pkg.devDependencies, 'typescript', ts);

              setIfPresent(pkg.dependencies, 'ng-zorro-antd', zorro);
              setIfPresent(pkg.devDependencies, 'ng-zorro-antd', zorro);

              fs.writeFileSync(pkgPath, JSON.stringify(pkg, null, 2) + '\n');
            };

            // patch root package.json
            patch(path.join(process.cwd(), 'package.json'));

            // patch all workspace project package.json files
            const projectsDir = path.join(process.cwd(), 'projects');
            if (fs.existsSync(projectsDir)) {
              for (const dir of fs.readdirSync(projectsDir)) {
                const pkgPath = path.join(projectsDir, dir, 'package.json');
                if (fs.existsSync(pkgPath)) patch(pkgPath);
              }
            }
          "

      - name: Debug pinned versions (v19/v20)
        if: matrix.angular != 21
        run: |
          node -p "require('./package.json').dependencies['@angular/core']"
          node -p "require('./package.json').dependencies['@angular/animations']"
          node -p "require('./projects/focusly-material/package.json').devDependencies['@angular/material'] || 'no material devDependency'"
          node -p "require('./projects/focusly-material/package.json').devDependencies['@angular/cdk'] || 'no cdk devDependency'"

      - name: Fresh install (v19/v20)
        if: matrix.angular != 21
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Build libraries
        run: npm run build:libs

  # playwright:
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: focusly

  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: npm
  #         cache-dependency-path: focusly/package-lock.json

  #     - name: Install deps
  #       run: npm ci

  #     - name: Build libs + demo (if needed)
  #       run: |
  #         npm run build:libs

      #- name: Install Playwright browsers
      #  run: npx playwright install --with-deps

      # - name: Run Playwright tests
      #   run: |
      #     set +e
      #     npx playwright test
      #     code=$?
      #     echo "Playwright exit code: $code"
      #     exit $code

      # - name: Upload Playwright HTML report
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: playwright-report
      #     path: focusly/playwright-report

      # - name: Upload Playwright traces/screenshots
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: playwright-test-results
      #     path: focusly/test-results